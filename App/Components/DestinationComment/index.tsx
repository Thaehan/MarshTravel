import { View, Text } from 'react-native'
import React from 'react'
import { IDestination } from '@Types/index'

export default function DestinationComment({data}: {data: IDestination}) {
  const {} = data;

  return (
    <View>
      <Text>DestinationComment</Text>
    </View>
  )
}